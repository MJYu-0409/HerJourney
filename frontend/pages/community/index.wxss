.placeholder {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100vh;
  background: var(--bg-primary);
  gap: 24rpx;
}
.ph-emoji { font-size: 100rpx; }
.ph-title { font-size: 44rpx; font-weight: 500; color: var(--text-primary); }
.ph-sub   { font-size: 28rpx; color: var(--text-secondary); }
